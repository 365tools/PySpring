"""
数据库会话管理（已废弃）

⚠️ 注意：此文件中的代码已废弃，仅作示例保留

PySpring v1.1.0+ 推荐使用框架提供的 DBManagerService：
- 自动管理数据库连接池
- 统一的会话管理
- 支持多数据库切换
- 集成到 IoC 容器

使用示例：
    from pyspring.repositories.db.manager import DBManagerService
    
    @Component()
    class MyRepository:
        def __init__(self, db_manager: DBManagerService):
            self.db = db_manager
        
        async def query(self):
            async with await self.db.session() as session:
                # 使用 session 进行数据库操作
                ...

数据库表初始化：
- 使用 MigrationInitializer（框架自动调用）
- 配置 config/repositories.yaml 中的 initialization 部分
- 支持 SQL 脚本或 SQLAlchemy metadata.create_all()
"""
